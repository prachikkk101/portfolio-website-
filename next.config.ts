import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Render deployment: Next.js automatically picks up the PORT env variable
  // No additional config needed for standard server (SSR) deployment
};

export default nextConfig;
